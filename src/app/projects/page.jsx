import ProjectContent from "@/components/ProjectContent";
import React from "react";

export const metadata = {
  title: 'Projects | Mohamed Tarek',
}

const page = () => {
  return (
    <ProjectContent />
  );
};

export default page;
